public static class UnitCoreModifiers
{
    /// <summary>
    /// Optional global owner for damage/buff modifiers (e.g. run-wide relic effects).
    /// </summary>
    public static object GlobalOwner { get; set; }
}
